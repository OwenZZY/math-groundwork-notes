
% Abstract (350 Words) 

\begin{abstract}
This is but a ``speech draft'' written by Zhiyuan Zhang (Owen) (he/him/his) for preparing for future potential teaching/lecturing positions.
\end{abstract}


\noindent{\bf List of Abbreviations and Symbols Used} \\ 
$G$ \dotfill graph\\
$V = V(G)$ \dotfill the vertex set of graph $G$\\
$E = E(G)$ \dotfill the edge set of graph $G$\\
$n = |V(G)|$ \dotfill the order of graph $ G $ and the dimension of square matrix\\
$m = ||E(G)||$ \dotfill the number of edges in graph $G$\\
$E(V_1, V_2)$ \dotfill{} the edge set that disconnects $V_1, V_2$ \\
$E(v)$ \dotfill{} the edge set that disconnects $v$ in $G$ \\
$[n]$ \dotfill the set of natural number from 1 to $n$\\
$k$ \dotfill dimension of threshold vectors\\
$A = (a_{i,j})_{i,j\in[n]}$\dotfill $n\times n$ matrix with real entry $a_{i,j}$\\
$\mathcal S^n[k]$ \dotfill the set of $n\times n$ Robinson matrices\\
$\bmd = (d_{i})_{i\in[k]}$\dotfill $k$-dimensional real column vector with entry $d_{i}$\\
%  \dotfill monotone decreasing vector of dimension $k$\\
$\bmD$ \dotfill set of all monotone decreasing vector of dimension $k$\\
$\chi_t$\dotfill one-hot $k$-dimensional vector with 1 on index $t$\\
$\bm 0$\dotfill $k$-dimensional zero vector\\
$\bm 1$\dotfill $k$-dimensional all-one vector\\
$\bma, \bmb, \bmc, \bm x, \bm y, \dots$\dotfill vectors\\ 
$u,v, u_-, v_-$ \dotfill vertices\\
% $A[I]$\dotfill submatrix of $A$ formed from rows and columns in index set $I$\\
$\subset$ \dotfill subset\\
$\subsetneq$ \dotfill proper subset
